#!/bin/bash
read -n 1 -p "Hello, are y sure that y want to run this (y/[a]): " AMSURE 
[ "$AMSURE" = "y" ] || exit
echo "" 1>&2
echo "this is your local time:"
T=$(date +%H:%M:%S)
D=$(date +%Y-%m-%d)
echo "$T" "$D"
